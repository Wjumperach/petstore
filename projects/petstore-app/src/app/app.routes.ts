import { Routes } from '@angular/router';

import { PetsContainerComponent } from './containers/pets-container/pets-container.component';

export const routes: Routes = [
    {
        path: '',
        component: PetsContainerComponent
    }
];
